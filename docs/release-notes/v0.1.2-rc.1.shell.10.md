# dsh-desktop v0.1.2-rc.1.shell.10

DeepSeek Harness 社区桌面版 · 内置内核 `@deepseek-ai/dsh` `0.1.2-rc.1`（未变，壳修订 +10）。

## 本次更新

- **修复手机会话在桌面重启后失效**：LAN 代理不再每次启动都更换设备会话签名密钥。配对过的手机可以跨共享停止/重开、Harness 恢复和桌面应用完整重启继续使用，无需重复输入配对码。
- **修复打包环境中的设备状态存储位置**：签名密钥与设备登记迁移到 Electron userData 的 `lan/` 私有目录，密钥权限收紧为仅当前用户可读写，不再尝试把设备状态写入随包资源目录。
- **补齐模拟器和支持诊断**：新增 Android 15 模拟器真实 Chrome 的端到端门禁，覆盖配对、后台恢复、共享重启和桌面重启；诊断报告增加不含密钥/配对码的 LAN 状态，Issue 模板也能直接收集手机与插件恢复阶段。
- **持续验证插件恢复**：真实 npm 市场再次跑通坏插件崩溃、自动隔离、升级并重新启用和 Harness 重启；打包 Safe Mode 继续验证故障插件隔离与恢复横幅。

## 验证

- 236 项单元测试、类型检查、运行时边界、覆盖率、官网契约与构建通过；完整本地发布门禁通过。
- Android 15 模拟器真实 Chrome 连续 3 次通过；移动 Chromium/WebKit、真实插件市场恢复、打包 Harness 与 Safe Mode 均通过。
- GitHub Release、GitCode 六项用户资产和官网发布数据将在发布工作流完成后复核。

## English Summary

- Preserved paired mobile sessions across LAN sharing, Harness recovery, and full desktop restarts by persisting the proxy signing key and device registry under private Electron user data.
- Added a real Android 15 emulator/Chrome journey covering pairing, background/resume, sharing restart, and desktop relaunch; LAN diagnostics expose connection facts without secrets or pairing codes.
- Revalidated the real-registry broken-plugin flow from crash and automatic quarantine through update, re-enable, and successful Harness restart, plus packaged Safe Mode recovery.
