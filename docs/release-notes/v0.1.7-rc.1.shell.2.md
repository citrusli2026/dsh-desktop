# dsh-desktop v0.1.7-rc.1.shell.2

DeepSeek Harness 社区桌面版 · 内置内核 `@deepseek-ai/dsh` 0.1.7-rc.1（未变，壳修订 +1）。

## 本次更新

- 修复内核 0.1.7 迁移 `settings.yaml` 后，原生菜单、窗口标题、托盘和通知语言回退的问题；优先从本地 `web` profile 读取语言与主题，并跟随 profile 更新。
- 保留对旧 `settings.yaml` 和 `.imported` 归档的兼容，读取归档时不会重建旧文件；偏好只保存在本机。
- 修正跨版本升级门禁：先在旧版首次启动前播种用户设置，再验证旧版迁移和新版安装都保留数据，避免测试夹具制造不真实的混合状态。

## 验证

- 本地 `verify`、双树安全审计、dev/packaged E2E、跨版本 macOS 升级冒烟、两种 packaged smoke、market offline/real 与 LAN QR 验证结果记录于 `HANDOFF.md` §64；Release CI 再执行 Windows NSIS 安装、升级与安装器完整性门禁。
- GitCode 新版附件公开下载仍受 §60–63 记录的平台级 404 影响；发布后按实际 Range GET 和官网数据核验并回填 `HANDOFF.md`。

## English Summary

- Preserve native menu, window, tray, and notification language and theme when Harness 0.1.7 migrates legacy settings by reading preferences from the local web profile.
- Keep compatibility with legacy settings and imported archives without recreating the old settings file; preferences stay on the device.
- Correct the cross-version upgrade fixture so user settings exist before the previous release's first launch and verify both migration and upgrade preservation.
