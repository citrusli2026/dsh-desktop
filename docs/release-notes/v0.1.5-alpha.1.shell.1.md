# dsh-desktop v0.1.5-alpha.1.shell.1

DeepSeek Harness 社区桌面版 · 内置内核 `@deepseek-ai/dsh` 0.1.5-alpha.1（内核升级，壳修订 +1）。

## 本次更新

- **内核升级**：随包 Harness 从 0.1.3-alpha.2 升至 0.1.5-alpha.1，同步全部 31 个直接 peer 契约、锁文件与供应链豁免清单。内核本版重构了会话持久化与终端子系统（新增 dsh-terminal、dsh-fs-local、dsh-session-projection 等子包）。
- **会话锁原生化简化**：新内核改用预编译的 `@deepseek-ai/node-addon-system`（稳定 Node-API，跨 ABI 兼容）承担文件锁，不再依赖需要按 Node ABI 重编译的 `fs-ext`。桌面壳的原生重建门禁改为按锁文件判定：内核仍携带 fs-ext 时照旧重编译并执行独占锁探针；内核不再携带时自动跳过；锁文件仍要求而闭包缺失时照旧报错拦截。
- **依赖安全地板**：修复两条新发布的高危通告（GHSA-rgj7-g3m4-5g8c、GHSA-2883-xcg3-v3hh）——构建链 sharp ≥0.35.4、内核图像附件链 sharp 0.35.4、插件/配置解析链 js-yaml 4.3.2、本地服务链 hono 4.13.5；根依赖树与 Harness 生产依赖树安全审计均回到 0 已知漏洞。
- **边界说明**：局域网配对密钥、设备登记、插件恢复、桌面垃圾桶和诊断仍只保存在本机，不上传用户数据。

## 验证

- 244 项单元测试（新增 fs-ext 门禁判定 5 项）；`verify` 全绿，16 项开发态 E2E、打包态 smoke、真实 Harness UI 渲染、离线与真实市场安装全部通过。
- 真实插件市场"崩溃 → 自动隔离 → 拉取最新版重启"链路在依赖地板之后复验 2/2 通过；本机 LAN 扫码配对二维码用例 2/2 通过。
- 随包 Node 22 直接加载 0.1.5-alpha.1 内核探针通过；发布工作流继续执行 macOS / Windows / Linux 构建、跨版本数据保留、8 资产契约与 attestation。
- GitCode 国内镜像和官网版本数据在三平台产物发布后同步并逐项在线校验。

## English Summary

- Upgraded the bundled Harness kernel to 0.1.5-alpha.1 and realigned all 31 direct peer contracts, the lockfile, and the supply-chain allowlist.
- The kernel now locks session files through the prebuilt Node-API `@deepseek-ai/node-addon-system`; the shell's native-rebuild gate consults the lockfile and rebuilds/probes fs-ext only when the kernel still ships it.
- Floors vulnerable transitive dependencies (sharp 0.35.4, js-yaml 4.3.2, hono 4.13.5) in both the build chain and the bundled kernel closure; both npm audit trees report zero known vulnerabilities.
- Revalidated real plugin installation with crash quarantine and update recovery, packaged Safe Mode, the real Harness UI, and the local LAN pairing QR gates.
