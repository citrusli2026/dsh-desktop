# dsh-desktop v0.1.7-rc.1.shell.3

DeepSeek Harness 社区桌面版 · 内置内核 `@deepseek-ai/dsh` 0.1.7-rc.1（壳修订 +1）。

## 本次更新

- 修复左下角 DeepSeek 账号菜单中的内置登录：获取授权地址后自动交由系统浏览器打开，并保留当前主题。
- 每次登录尝试最多自动打开一次授权地址；登录仍由 Harness 账号流程处理。

## 验证

- `pnpm run verify`、LAN 配对 E2E、macOS 打包版内置登录 E2E 均通过。
- GitHub Release、GitCode 镜像与官网数据将在发布流程中同步并验证。

## English Summary

- Fixed the built-in DeepSeek sign-in entry in the bottom-left account menu. The authorization URL now opens once per attempt in the system browser and keeps the current theme.
- Authentication remains in the Harness account flow.
